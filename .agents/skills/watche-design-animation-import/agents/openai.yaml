interface:
  display_name: "动画导入"
  short_description: "导入设计侧 GIF 动画，校验映射并生成 WatcheRobot AnimPack 资源。"
  default_prompt: "使用 $watche-design-animation-import 导入或诊断 WatcheRobot 设计动画资源。"

policy:
  allow_implicit_invocation: true
